$kind: http-request
name: GET /club/api/v1/users/:user_id/lessons
description: >-
  Auto-generated from Hotmart Developers docs (Gatsby `sample-code` extraction).


  Confidence: high.

  Verify path/query placeholders against the official docs and your environment
  `base_url`.
url: "{{base_url}}/club/api/v1/users/:user_id/lessons"
method: GET
queryParams:
  - key: subdomain
    value: "{{club_subdomain}}"
    description: "Required. Club subdomain (e.g. my-subdomain)."
pathVariables:
  user_id: ""
order: 4000
